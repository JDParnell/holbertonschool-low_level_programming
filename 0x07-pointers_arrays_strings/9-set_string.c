#include "holberton.h"

/**
 *set_string - takes a pointer and sets it to a char
 *@s: the string being set to the char
 *@to: the char the string is being set too
 *Return: void, no returns
 */

void set_string(char **s, char *to)
{
	*s = to;
}
