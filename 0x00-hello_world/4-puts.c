#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char tmp[] = "\"Programming is like building a multilingual puzzle";

	puts(tmp);
	return (0);
}
