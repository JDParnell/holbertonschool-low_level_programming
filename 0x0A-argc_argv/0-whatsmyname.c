#include "holberton.h"

/**
 *main - creates a program that prints the file name to the terminal
 *@argv: array of commands passed to program
 *@argc: number of commands
 *Return: always success, returns 0
 */

int main(int argc, char *argv[])
{
	argc += 0;
	printf("%s\n", argv[0]);
	return (0);
}
